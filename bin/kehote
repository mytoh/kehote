#!/usr/bin/env mosh

(import
  (scheme base)
  (scheme process-context)
  (prefix (kehote cli)
          cli.))

(define (main args)
  (cli.runner args)
  )

(main (command-line))

;; vim: filetype=scheme
